<div class="jumbotron center">
  <h1>Welcome to my app</h1>
  
  <% if user_signed_in? %>
  	# if the user is signed in display this 
  <% else %>
  	<!-- if the user is not signed in then display the following links --> 
  	<p>
  		<%= link_to "Log in", new_user_session_path, class: "btn btn-default btn-log" %>
  		<%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary btn-log" %>
  	</p>
  <% end %>
</div>

